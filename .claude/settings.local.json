{
  "permissions": {
    "allow": [
      "Bash(git push --force-with-lease origin feature/auth-headers)",
      "Bash(git fetch origin feature/auth-headers)",
      "Bash(git fetch origin)",
      "Bash(git push --force origin feature/auth-headers)",
      "Bash(npx tsc -p tsconfig.json --noEmit)",
      "Bash(./node_modules/.bin/tsc -p tsconfig.json --noEmit)",
      "Bash(./node_modules/.bin/vitest run)",
      "Bash(npm install @rollup/rollup-win32-x64-msvc)"
    ]
  }
}
